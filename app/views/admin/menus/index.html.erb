<p id="notice"><%= notice %></p>

<h1>Menus</h1>

<table>
  <thead>
    <tr>
      <th>T.integer</th>
      <th></th>
      <th>T.string</th>
      <th></th>
      <th>T.text</th>
      <th></th>
      <th>T.integer</th>
      <th></th>
      <th>T.integer</th>
      <th></th>
      <th>T.integer</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @menus.each do |menu| %>
      <tr>
        <td><%= menu.t.integer %></td>
        <td><%= menu. %></td>
        <td><%= menu.t.string %></td>
        <td><%= menu. %></td>
        <td><%= menu.t.text %></td>
        <td><%= menu. %></td>
        <td><%= menu.t.integer %></td>
        <td><%= menu. %></td>
        <td><%= menu.t.integer %></td>
        <td><%= menu. %></td>
        <td><%= menu.t.integer %></td>
        <td><%= menu. %></td>
        <td><%= link_to 'Show', menu %></td>
        <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
        <td><%= link_to 'Destroy', menu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Menu', new_menu_path %>
